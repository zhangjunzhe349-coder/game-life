# 依赖 / 缓存
node_modules/
*.log
.DS_Store
Thumbs.db

# 本地备份数据（不入库，避免记录外泄）
gamelife-backup-*.json
tools/preview-portrait.*
tools/pv-*.png
tools/browser-shot-*.png

# 部署产物（由 tools/build-dist.js 生成，是可重建的构建物，不入库）
dist/
